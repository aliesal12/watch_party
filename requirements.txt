# No third-party packages required.
# This project uses Python stdlib only (asyncio, http.server, urllib, json).
#
# You need:
#   - Python 3.10+  (for `X | Y` union type hints)
#   - VLC 3.x       (on both machines)
#
# Optional: if a video file seeks poorly when streamed (rare), you can remux
# it losslessly with ffmpeg:
#   ffmpeg -i input.mkv -c copy output.mkv
# ffmpeg is NOT required for normal operation.
